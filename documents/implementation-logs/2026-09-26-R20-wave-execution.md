# R20 実装ログ — wave単位の実行

- 実施日: 2026-09-26
- 対象: R20「wave単位の実行と狭い画面向けUIを整備する」の前半
- 状態: 前半完了、R20全体は未完了

## 実施内容

### フロントエンド

- 操作手順の命令グループを順にwave 1〜5として表示し、開始前に全waveか単独waveを選ぶUIを追加した。
- 対象未選択時は開始不可とし、操作手順の変更時には対象の再選択を必須にした。実行中は操作手順と実行対象の変更を無効にした。
- 実行エンジンが選択したwaveの命令だけを送信するよう変更し、元のグループ番号を維持したまま完了件数と進行表示を対象wave内で数えるようにした。
- 全waveの従来実行に加え、単独waveの送信・完了件数、操作手順の切り替えと実行中の変更禁止をテストした。既存の停止・再開・エラー関連テストも通過した。

### バックエンド

- 変更なし。既存の命令キューAPIと実行状態APIを利用した。

### プロジェクト共通

- ロードマップのR20を前半実装済みと記録し、全体のチェックは未完了のままとした。

## 検証結果

- Vitest: 8ファイル、53件成功。
- TypeScriptコンパイルとVite本番ビルド: 成功。
- ESLint、Prettier format check: 成功。
- `git diff --check`: 成功（改行コード変換の通知のみ）。

## 補足

- waveと命令グループは1対1で対応させた。シナリオJSONの形式は変更していない。
- 狭い画面向けの全体的なUI見直しと専用Chrome幅での実機確認はR20後半で行う。
- 旧アプリ `C:\2406_autoplay` のファイルには変更を加えていない。

## 次の作業

R20後半「狭い画面向けUIを整備する」へ進む。
