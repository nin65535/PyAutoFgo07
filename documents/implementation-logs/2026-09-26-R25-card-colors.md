# R25 実装ログ — 通常攻撃カードの位置と色

- 実施日: 2026-09-26
- 対象: R25「通常攻撃カード5枚の位置と色を検出する」
- 状態: 完了

## 実施内容

### バックエンド

- `autofgo.card_colors` に5枠の位置とHSVによるBuster・Arts・Quick判定を実装した。入力画像の座標系へ換算した矩形と色別の画素数を返す。
- カードの上下移動を縦方向の帯で吸収し、色が不明瞭なら例外で停止する。
- 合成画像で5色、上下移動、LDPlayer枠付き画像、未対応レイアウト、不明瞭な色を検証するテストを追加した。

### プロジェクト共通

- `documents/design/r25-card-colors.md` に座標系、色域、拒否条件、保存画像による検証範囲を記録した。
- 検証用の原画像をGit管理から除外し、ロードマップのR25を完了へ更新した。

## 検証結果

- Drive原本の戦闘0 `img1.png` と `img8.png` を保存し、正解ラベル10/10枚と一致した。Quickと効果表示を含む。
- pytest: 78件成功、1件対象外。サンドボックスでは一時ディレクトリ作成に失敗したため、権限付き環境で全件を実行した。
- Ruff lint: 成功。Ruff format check: 成功。`git diff --check`: 成功。

## 補足

- 画像はLDPlayerの既知レイアウト2種のみ自動判別する。その他はゲーム画面の矩形を明示する。
- 残り15画像の実画像評価や異なる解像度の評価は未実施。Drive READMEの85/85は既存の試行結果であり、本実装の検証結果として扱わない。
- 旧アプリは変更していない。
- StarletteとAnyIOの非推奨警告2件が表示された。

## 次の作業

R26「通常攻撃カードのキャラクター名を識別する」へ進む。
