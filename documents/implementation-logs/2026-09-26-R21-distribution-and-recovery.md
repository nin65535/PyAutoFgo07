# R21 実装ログ — 配布用起動と復旧手順

- 実施日: 2026-09-26
- 対象: R21「配布・起動・復旧手順を整備する」
- 状態: 完了

## 実施内容

### プロジェクト共通

- プロジェクト直下の`start-autofgo.cmd`をダブルクリックすると、`.venv`のPythonがビルド済み画面とAPIを同一ポートで配信し、専用Chromeを起動するようにした。
- 起動用コマンドプロンプトは実行中に最小化する。正常終了時は閉じ、異常終了時は復元してエラーメッセージを表示し、`pause`でキー入力を待つ。
- `README.md`に依存関係の導入、ビルド、起動、停止、更新を記載し、`documents/operations.md`にログ確認と復旧手順を記載した。

### フロントエンド・バックエンド

- FastAPIに静的画面の配信を追加し、開発時のViteを通常起動から外した。起動時にポートを使用できない場合は異常終了を通知する。

## 検証結果

- PowerShell起動スクリプトの構文解析、`git diff --check`: 成功。
- フロントエンドのビルド、ESLint、Vitest（55件）: 成功。
- バックエンドのRuff lint・format、pytest（74件）: 成功。
- ビルド済み画面のHTTP応答とAPI認証の簡易確認: 成功。
- ユーザーが実機でダブルクリック起動の成功を確認した。

## 補足

- 正常終了・異常終了時の起動用ウィンドウ挙動は個別に未検証。
- クリーン環境での再現は運用上想定しないため、ユーザーの指示に従いR21の要件から除外した。
- サンドボックスでViteの子プロセス起動が拒否されたため、ビルドとVitestは承認付き実行で検証した。
- 旧アプリ`C:\2406_autoplay`のファイルは変更していない。

## 次の作業

ロードマップの次項目は未定。必要な運用改善が判明した場合は新しい項目として追加する。
