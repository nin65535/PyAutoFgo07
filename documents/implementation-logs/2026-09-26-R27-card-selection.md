# R27 実装ログ — 固定優先順位によるカード選択

- 実施日: 2026-09-26
- 対象: R27「固定優先順位から選択するカードを決める」
- 状態: 完了

## 実施内容

### フロントエンド

- `attack('N1','B1','B1')` の文字列引数を解析し、3枠の `cardSlots` として型付き命令へ変換する。省略した枠は空文字で補完する。
- 宝具の重複、混合形式、不正な色や前衛番号を拒否する。数値形式は従来どおり保持する。

### バックエンド

- `cardSlots` を受信時に再検証する。従来の `noblePhantasmIndexes` と同時には指定できない。
- 識別済みの5枚から3枠へ割り当てる純粋関数を追加した。希望は3枠目から確保し、同条件なら画面左を選ぶ。未充足枠は1枠目から左側の未使用カードで補完する。
- 同名前衛の希望、不完全な認識結果、重複位置を拒否する。

## 検証結果

- バックエンド全テスト88件成功、1件対象外。Ruff lint と format check 成功。
- フロントエンドの全テスト61件成功。TypeScript `tsc -b`、ESLint、変更ファイルのPrettier checkが成功。
- サンドボックス内では一時ディレクトリを使う2件のセットアップに失敗したため、権限付き環境で全件を再実行した。

## 補足

- R26の識別器、およびR28の攻撃実行経路への接続は未実装。`cardSlots` の実行は明示的に失敗させ、誤って旧形式のカードを押さない。新形式の実運用はR28完了後に限る。
- 旧アプリは変更していない。

## 次の作業

R26「通常攻撃カードのキャラクター名を識別する」へ進む。
