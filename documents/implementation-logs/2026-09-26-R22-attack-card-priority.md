# R22 実装ログ — 攻撃カードの優先順位と互換仕様

- 実施日: 2026-09-26
- 対象: R22「操作手順JSONのカード優先順位と互換仕様を設計する」
- 状態: 完了（仕様設計）

## 実施内容

### プロジェクト共通

- `documents/design/r22-attack-card-priority.md` に新しい `attack` 記法、宝具と通常カードの選択順、左端補完、認識失敗時の停止条件を定義した。
- カードの数字は攻撃時点の前衛位置（0始まり）と定め、`swap()` 後の扱いと同名の前衛が複数いる場合の制約を記録した。
- 既存の `attack()` と数値引数を受け付け続け、従来の選択動作を維持する方針を明記した。
- `documents/roadmap2.md` のR22を完了へ更新し、作業詳細から仕様書へリンクした。

## 検証結果

- 既存のフロントエンドの解析器、バックエンドの型付きコマンド、攻撃処理と仕様の差分を確認した。
- `git diff --check` で差分の空白・改行を確認した。
- 実装コードを変更していないため、ビルド、静的解析、フォーマット、テストは実行していない。

## 補足

- 新記法の解析とカード選択は未実装。R27で型付きコマンドと選択処理、R28で画面操作への統合を行う。
- 新記法は宝具と通常カードを1本に連結する形式を採用した。既存シナリオファイルの変換は不要。
- 通常カードの認識に失敗した場合、誤選択を避けるため左端補完へ進まず停止する。
- 旧アプリ `C:\2406_autoplay` のファイルには変更を加えていない。

## 次の作業

R23「カード画像の参照データと正解データの形式を定義する」へ進む。
