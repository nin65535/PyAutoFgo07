# R24 実装ログ — カード参照画像サンプラー

- 実施日: 2026-09-26
- 対象: R24「スクリーンショットから識別用データを作る独立CLIサンプラーを実装する」
- 状態: 完了

## 実施内容

### バックエンド

- 独立CLI `autofgo.card_sampler` に `preview` と `register` を実装した。元画像の矩形を指定して候補PNGを出力し、人が確認した後にR23形式のPNGとmanifestへ登録できる。
- 画像をEXIF回転後のRGBとして扱い、範囲外の矩形、重複ID、既存PNGへの上書きを拒否する。
- プレビュー、登録、重複登録、範囲外のテストを追加した。

### プロジェクト共通

- `card-data/references/manifest.json` を作成し、アーラシュ、ダイダロス、バーヴァン・シーのステータス画面画像から各1件の参照PNGを登録した。
- `documents/design/r24-card-sampler.md` に確認、座標調整、追加登録、再作成の手順を記載した。
- `documents/roadmap2.md` のR24を完了へ更新した。

## 検証結果

- pytest: 76件成功、1件対象外。
- Ruff lint: 成功。
- Ruff format check: 成功。
- `git diff --check`: 成功。
- 戦闘0の `img1.png` と戦闘1の `スクリーンショット 2026-09-26 181746.png` を開き、3人の登録画像と同じカード絵柄が戦闘画面に現れることを目視確認した。

## 補足

- 数値スコアによる照合・識別精度はR26で検証する。今回の照合は目視確認であり、全戦闘画像への精度を示さない。
- 元のステータス画像と戦闘画像はGitに含めず、Drive原本を参照する。切り出しPNGのみGit管理する。
- pytestで一時ディレクトリの作成がサンドボックス内で拒否されたため、権限付き環境で全テストを実行した。StarletteとAnyIOの非推奨警告2件が表示された。
- 旧アプリ `C:\2406_autoplay` は変更していない。

## 次の作業

R25「通常攻撃カード5枚の位置と色を検出する」へ進む。
