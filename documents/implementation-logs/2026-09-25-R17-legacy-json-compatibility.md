# R17 実装ログ — 旧JSON資産の互換性検証

- 実施日: 2026-09-25
- 対象: R17「旧JSON資産の互換性を検証する」
- 状態: 完了

## 実施内容

### フロントエンド

- 変換済みシナリオ20ファイルを新スキーマで検証する回帰テストを追加した。
- 既存の限定文字列パーサーを使い、全310命令が型付きコマンドへ変換されることを確認した。
- 命令別の件数を`skill` 235件、`master_skill` 16件、`attack` 42件、`swap` 17件として固定し、資産の欠落や意図しない変更を検出できるようにした。

### プロジェクト共通

- 旧形式へ`schemaVersion: 1`を付与する`convert-legacy-scenarios.mjs`を追加した。
- 旧形式のルート項目を`members`と`commands`に限定し、未知項目や欠落を拒否するようにした。
- `C:\2406_autoplay\python\stages`の全20ファイルを変換し、`scenarios`へ新規ファイルとして保存した。
- 互換方針、変換手順、検証結果を`documents/design/legacy-json-compatibility.md`へ記録した。
- 基本設計と作業詳細の命令件数を、現在の旧資産の実数である310件へ訂正した。
- `documents/roadmap.md`のR17を完了へ更新した。

## 検証結果

以下の検証に成功した。

- 変換ツール実行: 20ファイル、310命令
- フロントエンド Vitest: 47件成功
- フロントエンド ESLint
- フロントエンド Prettier format check
- TypeScriptコンパイルおよびVite本番ビルド
- バックエンド pytest: 64件成功、OS資源を使うintegration 1件は通常設定どおり除外
- Ruff lintおよびformat check
- `git diff --check`

## 補足

- 初回計測時には313命令だったが、その後に旧JSONが編集されており、2026-09-25時点の旧資産は20ファイル、310命令だった。R17では現在の旧資産を対象として全ファイルを再集計し、変換後の回帰テストでも同じ件数を確認したため、資料を310件へ更新した。
- 旧命令はすべて既存の`skill`、`master_skill`、`attack`、`swap`の限定構文に適合した。危険な汎用構文の追加や手動修正は行っていない。
- 新アプリは旧JSONを直接受け入れず、変換済みの`schemaVersion: 1`形式だけを使用する。
- 旧アプリおよび`C:\2406_autoplay`内のJSONには変更を加えていない。
- pytestではFastAPI/Starlette依存関係由来の非推奨警告が2件表示されるが、テスト結果には影響しない。

## 次の作業

R18「自動テストを整備する」へ進む。
