# R21 実装ログ — ダブルクリック起動（途中）

- 実施日: 2026-09-26
- 対象: R21「配布・起動・復旧手順を整備する」の起動部分
- 状態: 起動ファイルを実装済み、R21全体は未完了

## 実施内容

### プロジェクト共通

- プロジェクト直下の`start-autofgo.cmd`をダブルクリックすると、`.venv`のPythonを使用してフロントエンド、バックエンド、専用Chromeを順に起動するようにした。
- 起動用コマンドプロンプトは実行中に最小化する。正常終了時は閉じ、異常終了時は復元してエラーメッセージを表示し、`pause`でキー入力を待つ。
- フロントエンド起動ログを`.autofgo/logs/frontend-launch.*.log`へ保存し、起動方法をREADMEへ記載した。

### フロントエンド・バックエンド

- アプリ本体の起動APIや配布形式は変更していない。既存の開発サーバーとPythonエントリーポイントを起動ファイルから使用する。

## 検証結果

- PowerShell起動スクリプトの構文解析: 成功。
- `git diff --check`: 成功（改行コード変換の通知のみ）。

## 補足

- ダブルクリックからの実際の起動、正常終了・異常終了時のウィンドウ挙動は未検証。
- 配布用の単一起動、クリーン環境での再現、復旧手順はR21の残作業。
- 旧アプリ`C:\2406_autoplay`のファイルは変更していない。

## 次の作業

R21で起動・停止・更新・ログ確認・専用Chromeプロファイル再作成と異常終了後の復旧手順を整備する。
